#include <iostream>
using namespace std;
#include<cmath>
int main()
{
    double a, b, c,area;
    cin >> a >> b >> c;
    if (a < 0 && b < 0 &&c < 0) {
        cout << "输入错误！" << endl;
    }
    else if (a + b > c && b + c > a&& a + c > b) {
        area=sqrt()
    }
    return 0;
}

